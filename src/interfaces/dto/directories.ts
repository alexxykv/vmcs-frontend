export interface DirectoryData {
  name: string
  directoryInJSON: string
  meetingId: string
  directoryZip: number[]
}

export interface CreateDirectoryData {
  name: string
  directoryInJSON: string
  meetingId: string
}