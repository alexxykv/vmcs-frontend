enum ApiRoutes {
  Auth = 'auth',
  ChannelInvitations = 'channelInvitation',
  Channels = 'channels',
  Directories = 'directories',
  Meetings = 'meeting',
  Users = 'user',
  Github = 'github'
}

export default ApiRoutes;